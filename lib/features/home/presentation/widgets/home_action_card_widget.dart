import 'package:flutter/material.dart';

class HomeActionCardWidget extends StatelessWidget {
  const HomeActionCardWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
