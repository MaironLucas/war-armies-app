import 'package:flutter/material.dart';

class HostRoomPage extends StatelessWidget {
  const HostRoomPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Host Room')));
}
