import 'package:flutter/material.dart';

class LobbyPlayerListWidget extends StatelessWidget {
  const LobbyPlayerListWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
