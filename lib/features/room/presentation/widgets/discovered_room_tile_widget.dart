import 'package:flutter/material.dart';

class DiscoveredRoomTileWidget extends StatelessWidget {
  const DiscoveredRoomTileWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
