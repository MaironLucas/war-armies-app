import 'package:flutter/material.dart';

class TroopsCounterWidget extends StatelessWidget {
  const TroopsCounterWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
